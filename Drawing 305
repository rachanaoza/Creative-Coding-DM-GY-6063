Pesudocode:
PROGRAM CalculatePoint
	The Eighth point is located halfway between the two points;
	An arc has its center at the first point;
	The arc has a radius equal to the distance between the first and the seventh points; 
	The arc would cross a line from the upper right corner to a point halfway between the midpoint of the bottom side and the lower right corner;
END



void setup(){
  size(600,600); // set screen size
  background(35); // set background color
  float a, b, c, d, start, stop; // set random values

  stroke(0,255,0); // stroke color for line
  strokeWeight(2); // stroke width for line
  line (600,0, 450, 600); // location of the line
  
  
  noFill(); // arc with no fill
  arc(400, 400, 300, 300, -HALF_PI, PI);
}

void draw(){
  }
  
